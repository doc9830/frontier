<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark shell: the WebView is the game, the window only frames it. -->
    <style name="Theme.Frontier" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/frontier_bg</item>
        <item name="android:colorBackground">@color/frontier_bg</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:colorAccent">@color/frontier_accent</item>
    </style>
</resources>
